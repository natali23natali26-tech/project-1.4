# 🛒 Модель данных интернет-магазина: Продукты и Категории

Простой, но мощный Python-модуль для работы с товарами и категориями в интернет-магазине.  
Реализует базовые сущности `Product` и `Category` с автоматическим подсчётом статистики.

---


## Установка:

Убедитесь, что на вашей системе установлен Python 3.8+

Команда для клонирования репрозитрия git clone https://github.com/natali23natali26-tech/project-1.4/tree/develop

Установка зависимостей: pip install -r requirements.txt


## 🚀 Основные возможности

✅ Создание товаров с валидацией цены

✅ Группировка товаров в категории

✅ Поддержка специализированных типов: смартфоны, газонная трава

✅ Автоматический подсчёт:

`category_count` — общее количество категорий

`product_count` — общее количество товаров

✅ Сложение товаров по стоимости запасов (product1 + product2)

✅ Метод new_product() — обновление или создание товара по данным

✅ Полная типизация (type hints), поддержка IDE

✅ Подходит для обучения, расширения и интеграции

✅ Логирование создания объектов через `LogCreationMixin`


## 📦 Логирование создания товаров

Классы продуктов используют миксин `LogCreationMixin`, который автоматически выводит информацию при создании нового объекта:

```
from src.products import Smartphone

smartphone = Smartphone("iPhone", "Флагман", 100000.0, 1, 98.5, "15 Pro", 512, "Чёрный")
# → Вывод: Smartphone('iPhone', 'Флагман', 100000.0, 1)
```

## 🔍 Особенности поведения

`LogCreationMixin` вызывает `print(repr(self))` в `__init__` базового класса Product

Это означает, что логирование происходит до полной инициализации подклассов

Например, при создании `Smartphone`:

Сначала вызывается `Product.__init__ → log_creation() → print(repr(self))`

На этом этапе поля `efficiency, model, memory, color` ещё не установлены

Однако `__repr__` использует имя подкласса: `Smartphone(...)`, а не `Product(...)`

💡 Важно: вывод всегда отражает тип объекта, но содержит только базовые поля `(name, description, price, quantity)`, так как дополнительные поля ещё не инициализированы.

## 🛠 Рекомендации по использованию

Если требуется логировать полные данные объекта, включая специфичные поля:

Убрать `self.log_creation()` из `Product.__init__`

Добавить вызов в конец __init__ подклассов:

```
def __init__(self, name, description, price, quantity, efficiency, model, memory, color):
    super().__init__(name, description, price, quantity)
    self.efficiency = efficiency
    self.model = model
    self.memory = memory
    self.color = color
    self.log_creation()  # логируем ПОСЛЕ полной инициализации
```

## 📚 API и примеры использования

**1. Создание товара**

```
from src.products import Product

phone = Product("Телефон", "Смартфон", 20000.0, 10)
print(phone)  # Телефон, 20000 руб. Остаток: 10 шт.
```

**2. Создание специализированного товара**

📱 Смартфон

```
from src.products import Smartphone

smartphone = Smartphone(
    name="iPhone 15",
    description="512GB, Gray space",
    price=210000.0,
    quantity=8,
    efficiency=98.2,
    model="15",
    memory=512,
    color="Gray space"
)
```

🌿 Газонная трава

```
from src.products import LawnGrass

grass = LawnGrass(
    name="Газонная трава",
    description="Элитная трава для газона",
    price=500.0,
    quantity=20,
    country="Россия",
    germination_period="7 дней",
    color="Зеленый"
)
```

**3. Работа с категориями**

```
from src.category import Category

category = Category("Смартфоны", "Высокотехнологичные смартфоны", [smartphone])

# Добавление нового товара
category.add_product(grass)  # ← вызовет TypeError: нельзя добавить не-Product

# Просмотр всех товаров
print(category.products)
# iPhone 15, 210000 руб. Остаток: 8 шт.

# Строковое представление категории
print(category)  # Смартфоны, количество продуктов: 8 шт.
```

**4. Сложение товаров (общая стоимость запасов)**

```
total_value = smartphone + grass  # TypeError — разные типы
total_value = smartphone + another_smartphone  # OK: 210000*8 + 180000*5 = 2580000
```

💡 Поддерживается sum():

```
total = sum([product1, product2, product3])  # Работает благодаря __radd__
```

**5. Метод new_product() — умное создание/обновление**

```
data = {
    "name": "Телефон",
    "description": "Обновлённая модель",
    "price": 25000.0,
    "quantity": 5
}

existing_products = [phone]  # уже есть Product с именем "Телефон"

updated_phone = Product.new_product(data, existing_products)
# → Обновит цену и увеличит количество, вернёт существующий объект
```

## Тестирование

Проект покрыт тестами с использованием pytest, parametrize, mock и patch.

Запуск тестов:

```
pytest tests/ -v --cov=src --cov-report=term-missing
```

## Документация:

Дополнительную информацию о структуре проекта и API можно найти в [документации](docs/README.md).

## Лицензия:

Проект распространяется под [лицензией MIT](LICENSE).
